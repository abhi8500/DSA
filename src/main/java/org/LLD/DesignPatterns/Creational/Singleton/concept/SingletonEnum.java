package org.LLD.DesignPatterns.Creational.Singleton.concept;

public enum SingletonEnum {

    /**
     * ENUM ensures the reflections to not override the no of instances.
     */
    SINGLETON_ENUM;
}
